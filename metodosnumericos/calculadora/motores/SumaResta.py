import numpy as np
import array as arr

def suma(self, matrixUno, matrixDos):
    mUno = np.array(matrixUno)
    mDos = np.array(matrixDos)
    return mUno + mDos

def resta(self, matrixUno, matrixDos):
    mUno = np.array(matrixUno)
    mDos = np.array(matrixDos)
    return mUno - mDos